import { GENRE_URL } from '../const';

export function loaderMovieByGenre({ request }) {
  // const url = new URL(request.url);
  // const searchTerm = url.pathname;

  return request
  // fetch(GENRE_URL + searchTerm)
  //   .then((res) => {
  //     if (res.ok) {
  //       return res.json();
  //     }

  //     throw new Error('Oh, No!!! Something went wrong: 💥');
  //   })
  //   .then((data) => data.data.movies)
  //   .catch((err) => {
  //     console.log(err);
  //   });
}