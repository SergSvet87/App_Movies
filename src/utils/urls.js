export const PATHS = {
  home: '/',
  about: '/about',
  teams: '/teams',
  // lessons: (textSearch = null) => (textSearch ? `lesson=${textSearch}` : 'lesson=react'),
  // category: (nameLesson) => (nameLesson ? `lesson/${nameLesson}` : 'lesson/:nameLesson'),
};


export const ALL_FILMS_URL = `https://yts.mx/api/v2/list_movies.json?limit=36&sort_by=year`

// ?genre=fantasy&sort_by=year&limit=33&minimum_rating=5.5